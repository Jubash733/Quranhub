name: library_domain
description: Library domain for Quran app.
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Resources
  resources:
    path: ../../resources/resources

  # Shared libraries
  common:
    path: ../../shared_libraries/common
  core:
    path: ../../shared_libraries/core
  dependencies:
    path: ../../shared_libraries/dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.12

flutter:
